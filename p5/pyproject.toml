[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fdi-pln-2602-p5"
version = "1.0"
description = "Mini LLM desde cero para la práctica 5 de PLN"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.4",
    "pandas>=3.0.3",
    "seaborn>=0.13.2",
    "torch>=2.11.0",
]

[project.scripts]
fdi-pln-2602-p5 = "app.cli:main"

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = [
    { index = "pytorch-cu128", marker = "sys_platform == 'linux'" },
]

[tool.setuptools]
include-package-data = true
packages = ["app", "app.attention", "app.chat", "app.inference", "app.model", "app.ner", "app.tokenizer", "app.train"]
